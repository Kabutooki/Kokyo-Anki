# v24 セッション演習監査

## 仕様
- デフォルト問題数: **20問**
- 任意の正整数を入力可能（教材総数を上限として正規化）
- 10 / 20 / 30 / 50問のクイック選択
- 候補問題が指定数より少ない場合は候補全問
- `setup` → `active` → `completed` の明示的状態遷移

## 開始
- setupでは問題カードを表示しない。
- 出題条件と候補数、今回の問題数を表示する。
- `演習開始` を押した時点でデッキを固定する。

## 演習中
- 4択回答のみで自動進行。
- モード、重要度、分野、検索、未学習のみ、シャッフル、履歴初期化、履歴読込はロック。
- `演習を終了` から途中終了できる。

## 終了
- 最終問題のフィードバック後、自動的に終了画面へ遷移。
- 回答数 / 正解 / 不正解 / 正答率を表示。
- 途中終了の場合は未回答数を明示。
- `同じ条件でもう一度` と `条件を選び直す` を用意。

## 後方互換
- v23以前の学習履歴（cardState / attemptEvents / confusionStats）は変更しない。
- v23以前にはsessionPhaseがないため、更新後は安全にsetupから開始する。
- v24で開始済みのactive sessionは、iPhone PWAの再起動・バックグラウンド復帰後も復元する。

## 画面確認
390×844pxのモバイルviewportで開始画面・終了画面をレンダリングし、問題数入力、クイック選択、開始ボタン、結果4指標、再演習／条件変更ボタンが1列表示内に収まることを目視確認した。
