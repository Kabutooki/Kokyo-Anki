# v27 「習熟以外」フィルター監査

## 仕様
- 学習メニューに `習熟以外` チェックボックスを追加。
- ON時は `statusOf(card) === 'mastered'` のカードのみ候補から除外する。
- `new`（未学習）、`review`（要復習）、`streak1`（連続正答1/2）、`due`（定着確認）は候補に残る。
- `due` は内部的には習熟履歴を持つが、復習期限が到来したカードであるため、間隔反復を妨げないよう「習熟以外」の対象に含める。
- `未学習のみ` と同時ONの場合、未学習カードだけが残る（集合の積）。
- `シャッフル` とは独立して利用できる。

## 状態管理
- `state.nonMasteredOnly` を追加。
- セッション状態へ保存・復元する。
- v26以前の保存状態にはこのフィールドがないため、復元時は `false` を既定値とする。
- 演習中は他の出題条件と同様に変更不可。

## データ非変更
- `cards.json` はv26とSHA-256一致。
- 問題文、正答、誤答、重要度、意味ベクトル、学習履歴スキーマに変更なし。

## 検査
- app.js 構文: PASS
- sw.js 構文: PASS
- VERSION.json / cards.json JSON構文: PASS
- checkbox DOM: PASS
- 保存・復元コード: PASS
- `mastered`除外ロジック: PASS
- `due`維持ロジック: PASS
- 演習中ロック: PASS
